// Section plane controls are rendered directly inside ViewportContainer as a
// screen-space overlay anchored to the 3D handle position.
// This file is kept as an empty re-export for backward compatibility.
export function ClipPlaneControl() { return null; }
